version: '3.8'

services:
  ingestion-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: metrics-ingestion-pipeline
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - WORKER_COUNT=4
      - QUEUE_CAPACITY=10000
    restart: always
